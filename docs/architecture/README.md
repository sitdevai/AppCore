# Architecture

AppCore uses Domain, Application, Infrastructure, Contracts, API, and React frontend boundaries. Generated applications must keep business rules out of endpoints and UI components and preserve backend authorization.
